define command{
        command_name    check_fs_rw
        command_line    sudo /usr/lib/nagios/plugins-cyb/check_fs_rw "$ARG1$"
}

define command{
        command_name    check_fs_rw_by_ssh
        command_line    ssh $HOSTADDRESS$ 'sudo /usr/lib/nagios/plugins-cyb/check_fs_rw "$ARG1$"'
}
